#!/usr/bin/env ruby

 require_relative '../lib/turn'
board  = [" ", " ", " ", " ", " ", " ", " ", " ", " "]
px = "X"
po = "O"
puts "Welcome to Tic Tac Toe!"
display_board(board)

xmove = gets.strip
input_to_index(xmove)
until valid_move?(board, xmove)
  xmove = gets.strip
  input_to_index(xmove)
end
turn(board, xmove, px)
